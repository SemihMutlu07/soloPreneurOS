---
phase: 07-dashboard-intelligence-feed
plan: "02"
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  - app/page.tsx
autonomous: false

requirements:
  - DASH-01
  - DASH-02
  - DASH-03
  - DASH-04
  - DASH-05
  - DASH-06
  - DASH-07
  - DASH-08

must_haves:
  truths:
    - "Home dashboard shows IntelligenceFeed in the bottom full-width row where ComingSoonAgents was"
    - "Navigating to / shows the intelligence feed card with AgentCardWrapper header and Refresh button"
    - "Feed card shows active insights with severity badges, timestamps, and evidence text"
    - "Clicking dismiss on a card removes it without page reload"
    - "Clicking Refresh triggers skeleton loading for ~3 seconds then updates cards"
    - "When no insights exist the empty state message is shown"
  artifacts:
    - path: "app/page.tsx"
      provides: "Dashboard page with IntelligenceFeed replacing ComingSoonAgents"
      contains: "IntelligenceFeed"
  key_links:
    - from: "app/page.tsx"
      to: "components/intelligence/intelligence-feed.tsx"
      via: "import and JSX in stagger-7 col-span-full row"
      pattern: "IntelligenceFeed"
---

<objective>
Wire IntelligenceFeed into the home dashboard and verify the complete feed experience in the browser.

Purpose: Plan 01 built the components in isolation; this plan makes them live on the dashboard by replacing the ComingSoonAgents section, then confirms every DASH requirement works end-to-end.

Output: Updated app/page.tsx and human confirmation that the feed behaves correctly.
</objective>

<execution_context>
@/home/parkermutsuz/.claude/get-shit-done/workflows/execute-plan.md
@/home/parkermutsuz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-dashboard-intelligence-feed/07-CONTEXT.md
@.planning/phases/07-dashboard-intelligence-feed/07-01-SUMMARY.md
</context>

<interfaces>
<!-- Prior plan output — executor reads these before modifying page.tsx -->

From app/page.tsx (current state before this plan):
```typescript
// Line 8 (import to remove):
import ComingSoonAgents from "@/components/agents/coming-soon-agents";

// Lines 62–68 (block to replace):
<div className="col-span-full opacity-0 animate-fade-in stagger-7 mt-2">
  <h3 className="text-xs font-medium text-text-muted uppercase tracking-wider mb-3">
    More agents coming soon
  </h3>
  <div className="grid grid-cols-1 md:grid-cols-2 gap-4">
    <ComingSoonAgents />
  </div>
</div>
```

From components/intelligence/intelligence-feed.tsx (created in Plan 01):
```typescript
export default function IntelligenceFeed()
// Self-contained: AgentCardWrapper header, Refresh button, skeleton, empty state, error state all inside
// No props needed — the section label comes from agentName="Cross-Module Intelligence" inside the component
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Wire IntelligenceFeed into app/page.tsx</name>
  <files>app/page.tsx</files>
  <action>
Read app/page.tsx in full, then make these two targeted changes:

CHANGE 1 — Remove ComingSoonAgents import:
Find the line: `import ComingSoonAgents from "@/components/agents/coming-soon-agents";`
Delete it entirely. Also add the IntelligenceFeed import in the agents import group:
`import IntelligenceFeed from "@/components/intelligence/intelligence-feed";`

CHANGE 2 — Replace the stagger-7 col-span-full block:
Find the block starting with:
```
<div className="col-span-full opacity-0 animate-fade-in stagger-7 mt-2">
```
that contains the `<h3>More agents coming soon</h3>` heading and `<ComingSoonAgents />`.

Replace the entire block (including its contents) with:
```tsx
<div className="col-span-full opacity-0 animate-fade-in stagger-7 mt-2">
  <IntelligenceFeed />
</div>
```

The IntelligenceFeed component is self-contained — the section heading ("Cross-Module Intelligence") is inside it via the AgentCardWrapper agentName prop. No external h3 needed.

Do not touch any other part of page.tsx.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>app/page.tsx compiles with zero TypeScript errors, ComingSoonAgents import is gone, IntelligenceFeed import is present, stagger-7 block renders only IntelligenceFeed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify complete feed experience in the browser</name>
  <action>Run `npm run dev`, open http://localhost:3000, and follow the verification steps below.</action>
  <files>none</files>
  <what-built>
    Complete intelligence feed on the home dashboard:
    - IntelligenceFeed replaces ComingSoonAgents in the bottom row
    - AgentCardWrapper header with "Cross-Module Intelligence" label and Refresh button
    - Insight cards with severity badges, freshness timestamps, evidence text, and dismiss buttons
    - Narrative (LLM) card rendered above rule cards (or empty state if none)
    - Skeleton loading during Refresh window
  </what-built>
  <how-to-verify>
1. Run `npm run dev` and open http://localhost:3000

2. Verify feed section exists:
   - Bottom of the dashboard shows a "Cross-Module Intelligence" agent card (AgentCardWrapper header with status dot and Refresh button)
   - No "More agents coming soon" section visible

3. Check initial load:
   - Insight cards render (or empty state "No cross-module patterns detected" if no pipeline run yet)
   - If cards present: severity badge shows (Critical / Warning / Info with correct colors), evidence text visible, freshness timestamp visible ("Xm ago" or "Xh ago")

4. Test dismiss (if insight cards are visible):
   - Click the X on any insight card
   - Card disappears immediately without page reload

5. Test Refresh button:
   - Click "Refresh" button in the feed header
   - Cards replaced with skeleton placeholders (~3 seconds)
   - Cards return after skeleton window (or empty state)
   - Button is disabled during the skeleton window

6. Verify TypeScript is clean: `npx tsc --noEmit` passes

7. Verify existing tests still pass: `npx vitest run`
  </how-to-verify>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>Human confirms: feed visible on dashboard at /, dismiss removes cards client-side, Refresh shows skeleton then reloads, TypeScript clean, vitest green.</done>
  <resume-signal>Type "approved" if feed works correctly, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
Phase gate — all must pass before /gsd:verify-work:
1. npx tsc --noEmit passes (zero errors)
2. npx vitest run passes (all server-side tests green — no regression from page.tsx change)
3. Human-verified: feed visible on dashboard, dismiss removes card, Refresh shows skeleton then reloads
</verification>

<success_criteria>
- app/page.tsx imports IntelligenceFeed, not ComingSoonAgents
- Home dashboard at / shows intelligence feed card in the bottom full-width row
- All DASH-01 through DASH-08 requirements observable in the browser
- TypeScript clean + all existing tests passing
</success_criteria>

<output>
After completion, create `.planning/phases/07-dashboard-intelligence-feed/07-02-SUMMARY.md`
</output>
