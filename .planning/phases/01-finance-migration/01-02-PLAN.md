---
phase: 01-finance-migration
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/seed-finance.ts
autonomous: true
requirements:
  - DATA-01

must_haves:
  truths:
    - "Running the seed script inserts invoices, expenses, and tax_provisions rows into Supabase"
    - "Running the seed script a second time does not create duplicate rows (idempotent)"
    - "After seeding, the Supabase invoices table has at least 6 rows matching the mock data"
  artifacts:
    - path: "scripts/seed-finance.ts"
      provides: "One-time seed of Finance mock data into Supabase via admin client"
      contains: "createAdminClient"
  key_links:
    - from: "scripts/seed-finance.ts"
      to: "supabase invoices/expenses/tax_provisions tables"
      via: "createAdminClient() upsert with onConflict: id"
      pattern: "upsert.*onConflict"
---

<objective>
Create a seed script that inserts the existing Finance mock data (invoices, expenses, tax_provisions from mock-data.ts) into Supabase as the initial dataset. The script is idempotent — running it twice does not duplicate rows.

Purpose: Bootstrap Supabase with realistic Finance data so the page has something to display immediately after the migration in Plan 03.
Output: scripts/seed-finance.ts — runnable with `npx ts-node scripts/seed-finance.ts` or `npx tsx scripts/seed-finance.ts`
</objective>

<execution_context>
@/home/parkermutsuz/.claude/get-shit-done/workflows/execute-plan.md
@/home/parkermutsuz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-finance-migration/01-CONTEXT.md

<interfaces>
<!-- Mock data to seed — extracted from lib/mock-data.ts -->

Finance mock data exports to seed:
- financeInvoices: Invoice[] — 6 invoices (inv-1 through inv-6). NOTE: These have string IDs like "inv-1". For seeding, map them to stable UUIDs so the seed is idempotent. Use a deterministic mapping: sha-based or just define a static UUID map in the script.
- financeExpenses: Expense[] — 5 expenses (exp-1 through exp-5)
- financeTaxProvisions: TaxProvision[] — 1 tax provision (tp-1)

The user_id in mock data is "user-1" (string). Replace with FINANCE_USER_ID = "00000000-0000-0000-0000-000000000001" (UUID) in the seed.

The Supabase tables use UUID primary keys. The seed script must supply explicit UUIDs (not let Supabase auto-generate) so re-running does not create duplicates. Use upsert with onConflict on id column.

Stable UUID mapping for invoices (use these exact values):
  inv-1 → "00000000-0000-0000-0001-000000000001"
  inv-2 → "00000000-0000-0000-0001-000000000002"
  inv-3 → "00000000-0000-0000-0001-000000000003"
  inv-4 → "00000000-0000-0000-0001-000000000004"
  inv-5 → "00000000-0000-0000-0001-000000000005"
  inv-6 → "00000000-0000-0000-0001-000000000006"

Stable UUID mapping for expenses:
  exp-1 → "00000000-0000-0000-0002-000000000001"
  exp-2 → "00000000-0000-0000-0002-000000000002"
  exp-3 → "00000000-0000-0000-0002-000000000003"
  exp-4 → "00000000-0000-0000-0002-000000000004"
  exp-5 → "00000000-0000-0000-0002-000000000005"

Stable UUID mapping for tax_provisions:
  tp-1 → "00000000-0000-0000-0003-000000000001"

From lib/supabase/admin.ts:
```typescript
export function createAdminClient() {
  return createClient(
    process.env.NEXT_PUBLIC_SUPABASE_URL!,
    process.env.SUPABASE_SERVICE_ROLE_KEY!,
  );
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write idempotent Finance seed script</name>
  <files>scripts/seed-finance.ts</files>
  <action>
Create scripts/seed-finance.ts:

1. Load env vars from .env.local using dotenv (add `import dotenv from "dotenv"; dotenv.config({ path: ".env.local" });` at top). If dotenv is not in package.json, use `process.env` directly (the Next.js dev server exposes them — alternatively note that the user may need to run with dotenv-cli).

2. Import createAdminClient from "../lib/supabase/admin".

3. Define FINANCE_USER_ID = "00000000-0000-0000-0000-000000000001".

4. Define the seed data inline — do NOT import from lib/mock-data.ts directly (the script runs outside Next.js context and path aliases may not resolve). Instead, copy the relevant data as plain objects with the stable UUIDs mapped in (see interface section above for UUID map).

5. Seed invoices: supabase.from("invoices").upsert(invoiceRows, { onConflict: "id" })
   Each row: { id: <stable UUID>, user_id: FINANCE_USER_ID, client_name, client_vkn, description, gross_amount, kdv_rate, kdv_amount, stopaj_rate, stopaj_amount, net_amount, invoice_type, status, created_at }

6. Seed expenses: supabase.from("expenses").upsert(expenseRows, { onConflict: "id" })
   Each row: { id: <stable UUID>, user_id: FINANCE_USER_ID, description, amount, kdv_paid, category, date, created_at }

7. Seed tax_provisions: supabase.from("tax_provisions").upsert(taxRows, { onConflict: "id" })
   Each row: { id: <stable UUID>, user_id: FINANCE_USER_ID, period, kdv_payable, gecici_vergi_estimate, sgk_amount, total_provision, created_at }

8. Log success/error counts to console. Exit with process.exit(1) on any error.

Run instructions (add as comment at top of file):
// Run: npx dotenv-cli -e .env.local -- npx tsx scripts/seed-finance.ts
// Or: NEXT_PUBLIC_SUPABASE_URL=... SUPABASE_SERVICE_ROLE_KEY=... npx tsx scripts/seed-finance.ts
  </action>
  <verify>
    <automated>cd /home/parkermutsuz/dev/soloPreneurOS && npx tsc --noEmit scripts/seed-finance.ts 2>&1 || echo "check output above for type errors"</automated>
  </verify>
  <done>scripts/seed-finance.ts exists, compiles without TypeScript errors, contains upsert calls with onConflict: "id" for all three tables</done>
</task>

</tasks>

<verification>
Verify the script file exists and has the correct structure:
```bash
grep -c "upsert" /home/parkermutsuz/dev/soloPreneurOS/scripts/seed-finance.ts
# Should output 3 (one per table)
grep "onConflict" /home/parkermutsuz/dev/soloPreneurOS/scripts/seed-finance.ts
# Should show onConflict: "id" pattern
```

To actually run against Supabase (requires SUPABASE_SERVICE_ROLE_KEY in .env.local):
```bash
cd /home/parkermutsuz/dev/soloPreneurOS && npx dotenv-cli -e .env.local -- npx tsx scripts/seed-finance.ts
```
</verification>

<success_criteria>
- scripts/seed-finance.ts exists
- Seeds all 6 invoices, 5 expenses, 1 tax_provision using upsert with onConflict: "id"
- Uses stable UUID IDs so re-runs are idempotent (no duplicates)
- Uses FINANCE_USER_ID = "00000000-0000-0000-0000-000000000001" for all user_id fields
- Uses createAdminClient() — no RLS dependency
- TypeScript compiles cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/01-finance-migration/01-02-SUMMARY.md`
</output>
